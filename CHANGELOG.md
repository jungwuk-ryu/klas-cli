## 0.2.0

- Improve local auth durability with stored credential reuse and automatic session recovery.
- Recover more safely from invalid stored session state and add regression coverage for auth persistence.
- Publish `klasflow` as a hosted dependency, add one-line installers, and move public docs under `doc/`.
- Update agent-facing and public auth documentation to match the installed CLI workflow.
- Fix `courses show` usage handling for `--course` and correct the `tasks show` help usage line.

## 0.1.0

- Initial version.
