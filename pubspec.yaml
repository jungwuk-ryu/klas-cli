name: klas_cli
description: Agent-first Dart CLI for KLAS built on top of klasflow.
version: 0.2.0
repository: https://github.com/jungwuk-ryu/klas-cli

environment:
  sdk: ^3.11.1

executables:
  klas: klas

dependencies:
  args: ^2.7.0
  cryptography: ^2.7.0
  http: ^1.5.0
  klasflow: ^1.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
